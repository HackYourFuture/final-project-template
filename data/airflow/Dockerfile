# Astro runtime, the same image family you used in Week 12.
#
# Start locally:  astro dev start
# It prints the UI address when it comes up. Recent Astro CLI versions use
# http://airflow.localhost:6563 rather than the port 8080 you saw in Week 12.
FROM astrocrpublic.azurecr.io/runtime:3.3-2
