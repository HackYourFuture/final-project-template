# Copy to .env before `docker compose up`. Never commit .env.
#
# These three drive the Postgres container, the backend's datasource and the
# data pipeline's publish step, so every part of the stack agrees on one
# database.
POSTGRES_DB=finalproject
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
