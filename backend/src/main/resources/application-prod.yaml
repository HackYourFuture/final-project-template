spring:
  datasource:
    # e.g: jdbc:postgresql://server:5432/mydb
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

logging:
  level:
    root: INFO

# Add any additional configuration for the production environment